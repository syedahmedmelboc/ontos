---
phase: 02-typography-logo
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/frontend/tailwind.config.cjs
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
must_haves:
  truths:
    - "Tailwind config references VIC font family"
    - "Body text uses VIC font with Arial fallback"
    - "font-normal, font-semibold, font-bold map to VIC weights"
  artifacts:
    - path: "src/frontend/tailwind.config.cjs"
      provides: "Font family configuration for Tailwind"
      contains: "fontFamily"
  key_links:
    - from: "tailwind.config.cjs fontFamily"
      to: "@font-face declarations"
      via: "font-family name 'VIC'"
      pattern: "fontFamily.*VIC"
---

<objective>
Update Tailwind CSS configuration to use VIC font family with Arial fallback for all typography.

Purpose: Ensure all Tailwind typography utilities render with Brand Victoria fonts.
Output: Updated tailwind.config.cjs with fontFamily configuration.
</objective>

<execution_context>
@/Users/syedahmed/repos/newrepos/ontos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/syedahmed/repos/newrepos/ontos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-typography-logo/02-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/frontend/tailwind.config.cjs:
```javascript
module.exports = {
  darkMode: ["class"],
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    container: { ... },
    extend: {
      colors: { ... },
      borderRadius: { ... },
      keyframes: { ... },
      animation: { ... }
    }
  },
  plugins: [require('@tailwindcss/typography'),require("tailwindcss-animate")],
}
```

Font weight mappings (standard Tailwind):
- font-normal = 400 (maps to VIC-Regular/Book)
- font-semibold = 600 (maps to VIC-SemiBold)
- font-bold = 700 (maps to VIC-Bold)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add fontFamily to Tailwind config</name>
  <files>src/frontend/tailwind.config.cjs</files>
  <read_first>
    - /Users/syedahmed/repos/newrepos/ontos/src/frontend/tailwind.config.cjs
  </read_first>
  <action>
    Add fontFamily configuration to the `theme.extend` object in tailwind.config.cjs.

    Add this property to `theme.extend` (after `colors` or at the beginning of extend):

    ```javascript
    fontFamily: {
      sans: ['VIC', 'Arial', 'sans-serif'],
    },
    ```

    The exact location in the extend object should be:

    ```javascript
    extend: {
      fontFamily: {
        sans: ['VIC', 'Arial', 'sans-serif'],
      },
      colors: {
        // ... existing colors
      },
      // ... rest of extend
    }
    ```

    Key details:
    - fontFamily key: 'sans' (Tailwind's default sans-serif family)
    - Font stack: ['VIC', 'Arial', 'sans-serif']
    - 'VIC' matches the font-family name in @font-face declarations
    - 'Arial' is the official Brand Victoria fallback font
    - 'sans-serif' is the final browser fallback
  </action>
  <verify>
    <automated>grep -A3 "fontFamily:" src/frontend/tailwind.config.cjs | grep -q "VIC.*Arial.*sans-serif" && echo "Font family configured"</automated>
  </verify>
  <done>
    - tailwind.config.cjs contains fontFamily in theme.extend
    - fontFamily.sans = ['VIC', 'Arial', 'sans-serif']
    - Font stack matches Brand Victoria guidelines
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify font weight mappings</name>
  <files>src/frontend/tailwind.config.cjs</files>
  <read_first>
    - /Users/syedahmed/repos/newrepos/ontos/src/frontend/tailwind.config.cjs
  </read_first>
  <action>
    Confirm that Tailwind's default font-weight values align with VIC font files:

    No changes needed - Tailwind defaults already match:
    - font-normal (400) -> VIC-Regular (Book)
    - font-semibold (600) -> VIC-SemiBold
    - font-bold (700) -> VIC-Bold

    This task is verification-only. If any custom fontWeight configuration exists, ensure it does not conflict with the VIC weights (400, 600, 700).

    Check that no `fontWeight` property exists in theme.extend that would override defaults. If one exists, verify values 400, 600, 700 are correct.
  </action>
  <verify>
    <automated>grep -c "fontWeight:" src/frontend/tailwind.config.cjs || echo "No fontWeight override (using Tailwind defaults)"</automated>
  </verify>
  <done>
    - Tailwind default font-weights (400, 600, 700) will render VIC fonts correctly
    - No conflicting fontWeight configuration exists
  </done>
</task>

</tasks>

<verification>
- [ ] fontFamily.sans = ['VIC', 'Arial', 'sans-serif'] in tailwind.config.cjs
- [ ] No conflicting fontWeight configuration
- [ ] Tailwind build succeeds (fonts load without errors)
</verification>

<success_criteria>
- Tailwind config references VIC font family with Arial fallback
- All text using Tailwind typography utilities renders with VIC fonts
- font-normal, font-semibold, font-bold map correctly to VIC weights
</success_criteria>

<output>
After completion, create `.planning/phases/02-typography-logo/02-02-SUMMARY.md`
</output>
